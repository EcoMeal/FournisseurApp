{% extends "::index.html.twig" %}

{% block body %}
    
    {% if error is defined %}
	
	<p> {{error}} </p>
	
    {% endif %}

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Valider</button>
{{ form_end(form) }}

{% endblock %}